<!--This page should list all of the breeds that are in the database.  
		EX: Airedale terrier
				Border collie
				Rabbit
		Each breed should appear only once even if multiple indvidual animals share that breed. Each breed should be linked to a page which lists all of the
		individual animals who share that breed. Add and Edit breed links? -->
		<h1>Master List of Breeds and Animal types</h1>
		<h3>Types of animals in our database.</h3>
		<p>Click on a breed/type to see the indvidual animals of that type.</p>
		<p><%= link_to 'Add a breed', new_breed_path %></p>
			<table>
				
			<tbody>
					<% @breeds.each do |breed| %>
					<tr><%= link_to breed.breed, breed_path(breed.breed) %></tr>
			</tbody>
		</table>

	</br>
<% end %>